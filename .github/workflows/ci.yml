name: ci

on:
  push:
  pull_request:

jobs:
  config-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run config validation
        run: bash ./ops/scripts/validate-config.sh
