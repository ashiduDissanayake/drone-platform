# AWS SITL Inventory
# Generated by Terraform
# Usage: ansible-playbook -i inventory/aws.yml site.yml

all:
  children:
    sim_host:
      hosts:
        sitl-aws:
          ansible_host: ""  # Fill in with: terraform -chdir=../terraform output -raw sitl_public_ip
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ../terraform/sitl-key.pem
          ansible_python_interpreter: /usr/bin/python3
          ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
