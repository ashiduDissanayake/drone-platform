# Mission Catalog

Mission scenarios referenced by deployments and profiles live in this directory.

- Filename and `metadata.name` should match the scenario ID.
- Validation checks that referenced missions exist here.
